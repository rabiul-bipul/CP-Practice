#include <bits/stdc++.h>
using namespace std;

int main()
{
    int n, t;
    scanf("%d %d", &n, &t);
    int arr[n];

    for (int i = 0; i < n; i++)
    {
        scanf("%d", &arr[i]);
    }
    for (int i = n - t; i < n; i++)
    {
        printf("%d ", arr[i]);
    }
    for (int i = 0; i < n - t; i++)
    {
        printf("%d ", arr[i]);
    }
    printf("\n");
}
